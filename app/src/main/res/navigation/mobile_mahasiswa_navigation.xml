<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_mahasiswa_navigation"
    app:startDestination="@+id/navigation_mahasiswa_home">

    <fragment
        android:id="@+id/navigation_mahasiswa_home"
        android:name="com.myapp.android.pasti.mahasiswa.MahasiswaHomeFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_mahasiswa_home" />

    <fragment
        android:id="@+id/navigation_mahasiswa_faq"
        android:name="com.myapp.android.pasti.mahasiswa.MahasiswaFAQFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_mahasiswa_faq" />

    <fragment
        android:id="@+id/navigation_mahasiswa_profile"
        android:name="com.myapp.android.pasti.mahasiswa.MahasiswaProfileFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_mahasiswa_profile" />
</navigation>
